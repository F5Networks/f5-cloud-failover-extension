{
  "primary": {
    "admin_password": "${admin_password}",
    "admin_username": "${admin_username}",
    "mgmt_address": "${primary_ip_addr}"
  },
  "secondary": {
    "admin_password": "${admin_password}",
    "admin_username": "${admin_username}",
    "mgmt_address":"${secondary_ip_addr}"
  }
}